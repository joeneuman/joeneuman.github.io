for (var i = 0; i < animals.length; i++) {
  for (var j = 0; j < products.length; j++) {
    foodItems[k] = animals[i] + products[j];
    
  }
}
for (var i = 0; i < animals.length; i++) {
  for (var j = 0; j < products.length; j++) {
    foodItems[k] = animals[i] + products[j];
    k++;
  }
} 

  for (var j = 0; j < 10; j++) {
  }
  
  
  // Code the first line of an if statement that tests whether a segment with an index represented by indexNum exists in a string.
  
  if (indexNum !== -1) { 

// 11) Code an alert that displays the index number of the first character of a segment represented by a variable that occurs within a string represented by a second variable. 
    
    alert(secondVar.indexOf(be));

// 12) Code the first line of an if statement that tests whether a 3-character slice at the beginning of a string represented by one variable can be found somewhere within a string represented by a second variable.

if (var2.indexOf(var1.slice(0, 3)) !== -1) { 
  
var indx = text.indexOf(seg);



11)
if (vrS.charAt(2) === "a") {
if (str.charAt(2) === "a") {
  
var x = str.charAt(0);
   

var indx = text.indexOf("seg");

// 15)
var indx = text.lastIndexOf("seg");

if (indexNum !== -1) {
  
alert(stringV.indexOf(seg));

if (var2.indexOf(var1.slice(0, 3)) !== -1) {
  
for (var i = 0; i < array.length; i++) {
  var element = array[i];
  
}


for (var i = 0; i < text.length; i++) {
 textArr[i] = text.charAt(i);
}
for (var i = 0; i < text.length; i++) {
  textArray[i] = text.charAt(i);
  
  
} 
  
var comOfVar = striVar.charAt(0, 1) + anoString.charAt(anoString - 1)

var combo = str1.charAt(0) + str2.charAt(str2.length - 1); 

